{
  "name": "CountriesService",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "webApp",
    "countries-service/build/dist/js/developmentLibrary"
  ],
  "scripts": {
    "build:shared": "./gradlew :countries-service:jsBrowserDevelopmentLibraryDistribution",
    "build": "npm run build:shared && npm run build -w webApp",
    "start": "npm run build:shared && npm run start -w webApp"
  }
}